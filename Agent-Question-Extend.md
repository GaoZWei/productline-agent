1.Q01 说解决 为什么订单还没有交付”这类需要跨订单、生产、质检、复核和交付等多个业务模块进行取证的问题，还解决哪种问题？？

个人思考：
一些知识库问答、


2.与现成的java接口，如何进行适配
做一个适配层还是直接调用，调用返回的值怎么有效转化成agent需要的格式


3.动态诊断为什么适合图

初始化
→ 模型选择下一步动作
→ 校验动作
→ 执行 Tool
→ 保存观察
→ 检查信息是否充分
   ├─ 不充分：回到模型继续决策
   ├─ 充分：生成规则诊断
   └─ 异常或预算耗尽：进入安全出口

这里存在三个普通线性函数不容易直接表达清楚的特点：
1. 多轮回环：每取得一项新事实，都可能重新决定下一步查询什么；
2. 共享状态：订单、任务、质检、复核、交付事实和调用历史需要在多轮之间累积；
3. 多种停止原因：信息充分、模型主动结束、重复调用、无新信息、调用预算耗尽和执行异常需要区分。

这些逻辑当然也可以写成 while 循环加大量 if/else，但随着状态和停止条件增加，循环控制、异常出口和状态修改会集中在一个函数中。LangGraph 将它们拆成明确节点，使每个节点只负责一类行为，也更容易单独验证路由和停止条件。

4.多Agent的必要性：
子任务是否真的需要独立规划、独立上下文和协作关系。