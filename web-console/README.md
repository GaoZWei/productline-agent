# Web Console

M0.1 提供的 Web 可启动骨架。当前用于验证端口和容器编排，Vue 3/Vite 工程将在 T050 初始化。

使用 Docker 启动：

```bash
docker compose up --build web-console
```

使用本机 Node.js 启动：

```bash
cd web-console
PORT=5173 node server.mjs
```

