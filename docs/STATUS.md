# 当前开发状态

- 当前里程碑：M2 确定性订单诊断（进行中）
- 当前子阶段：M2.9 前端 Agent 侧边栏（T258～T267，已完成）
- 已完成任务：T001～T153、T201～T267
- 当前场景：订单业务页可打开诊断侧边栏，自动携带当前订单并提交用户问题；页面展示加载状态、阻塞环节、根因、Tool字段级证据、处理建议、Run/Trace及结构化错误，订单切换会丢弃旧请求结果
- 通过测试：Web 6个测试文件、13/13通过，Vue TypeScript检查和Vite生产构建通过；此前M2.8 API专项6/6、Python汇总238通过/18个数据库集成用例按门禁跳过、隔离PostgreSQL持久化与API共23/23、Ruff和mypy strict（53个文件）结果保持有效，本次未重复无关模块
- 失败测试：最终结果0；本次首次运行Web测试、类型检查和生产构建均通过
- 当前阻塞：无
- 开发环境：OpenJDK 21.0.12、Maven 3.9.16、Python 3.12.13（uv 管理）、uv 0.12.0、Node.js 22.22.2、npm 10.9.7、Docker Desktop 29.6.2
- 最近更新：M2.9增加独立Agent HTTP客户端、开发与生产同源代理、当前订单诊断抽屉、加载/成功/错误结果展示及防串单处理
- 已知非阻塞问题：前端演示身份Header和一次性Session分别只是最小身份上下文与运行归属，不是完整认证或多轮会话；诊断仍为单次HTTP响应，没有SSE，建议只展示且不能确认执行；未预期节点异常可能遗留RUNNING Step，尚无崩溃恢复；具体模型客户端、Prompt版本和真实模型评测未装配；没有Step重试次数/Trace持久化或跨实例调用账本；调试Run与持久化Run仍未关联；本地Java/Python数据库角色未隔离；Java测试仍有Mockito动态Agent的未来JDK兼容警告
- 下一任务：T268～T275 实现 M2.10 五单、资源不存在、超时和字段错误E2E测试
