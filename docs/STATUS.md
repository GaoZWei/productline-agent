# 当前开发状态

- 当前里程碑：M5 动态异常诊断Agent（进行中）
- 当前子阶段：M5.3 LangGraph 动态图（T515～T523，已完成）
- 已完成任务：T001～T153、T201～T275、T301～T354、T401～T487、T501～T523
- 当前场景：固定订单诊断保持原有路径；动态诊断现可在LangGraph中循环执行“规划、校验、只读Tool/RAG调用、Observation保存和完成判断”，Java事实与规范结果分通道保存，`FINISH`由确定性规则区分信息充分或不足，执行失败进入异常结束且不伪造业务结论
- 通过测试：Python汇总415通过/38个需外部环境的用例按门禁跳过；M5.3动态成功、主动结束和Tool异常3/3，相关契约与诊断生成20/20；mypy strict（112个源文件）通过；M5.3新增文件Ruff和格式检查通过；此前真实跨服务E2E最近一次8/8、M4知识与RAG测试91/91、Web 18/18及生产构建、Compose健康检查与配置校验结果保持有效
- 失败测试：开发中先以缺少`DynamicDiagnosisWorkflow`导出验证新测试有效；首轮发现无任何Tool事实时旧的信息不足生成器要求伪造至少一条证据，现已改为仅`INSUFFICIENT_INFORMATION`允许空证据；全量Ruff仍被5个既有文件的44项注释标点/行长问题拦截，全量格式检查仍有72个既有文件待格式化
- 当前阻塞：无
- 开发环境：OpenJDK 21.0.12、Maven 3.9.16、Python 3.12.13（uv 管理）、uv 0.12.0、Node.js 22.22.2、npm 10.9.7、Docker Desktop 29.6.2
- 最近更新：M5.3新增八节点动态LangGraph，动作执行前复核LOW风险、参数和资源归属，Java Tool事实与规范问答结果隔离保存，Observation记录安全摘要和指纹，并增加信息不足及执行异常终止原因
- 已知非阻塞问题：M5.3尚无具体动作模型供应商，尚未实现M5.4最大轮次、Tool调用数、重复动作、连续无新增信息和错误次数限制；全量Ruff存在5个既有文件的44项注释标点/行长问题，全量格式检查存在72个既有未格式化文件；M4.12可控Subject用于验证数据契约、策略执行和指标数学，不代表尚未接入的真实Embedding/Reranker质量，默认低相关阈值0.5和RRF`k=60`仍需真实Provider评测校准；规范问答仍是内部组件，尚未接入统一路由HTTP、页面问答交互、Run/Step持久化或具体问答/Rerank模型客户端；当前Query Rewrite只做NFKC和空白规范化，不处理别名、缩写或多轮指代；页面产品/卫星元数据只作为收窄提示且未由Java事实重校验，权限和生效日期仍必须显式提供；回答模型引用ID经过白名单校验，但尚无自动事实一致性或声明级引用覆盖评测；当前权限枚举只有`INTERNAL_REVIEWER`，50条评测均为DOM/GF-2/内部复核范围，尚无第二产品、卫星或权限范围的标注对比；当前检索只返回`ACTIVE`规范，不支持历史审计as-of查询；M4.5双字词元可能产生词义无关共同双字召回，M4.6 HNSW尚无大规模召回率和延迟评测；尚未提供全目录入库CLI、定时任务或HTTP管理入口；当前token数是不绑定供应商的确定性近似值，重复检测只判断规范化正文完全相同，第一版不支持PDF；路由组件仍无FastAPI统一入口、持久化路由Run/Step或真实自然语言分发；TTL无后台物理清理，Session无归档策略；任务与质检页面尚未建设，`batch_id`和`satellite_type`缺少Java事实重校验契约；演示身份Header不是完整认证；诊断没有SSE或Approval，未预期节点异常可能遗留RUNNING Step；没有Step重试次数/Trace持久化或跨实例调用账本；本地Java/Python数据库角色未隔离；Java测试仍有Mockito动态Agent的未来JDK兼容警告
- 下一任务：T524～T531 实现M5.4 Agent执行限制，包括最大轮次、Tool调用数、重复调用、连续无新增信息、错误上限和异常动作拦截
