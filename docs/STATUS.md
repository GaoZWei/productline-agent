# 当前开发状态

- 当前里程碑：M3 页面上下文与路由（进行中）
- 当前子阶段：M3.2 会话上下文（T310～T318，已完成）
- 已完成任务：T001～T153、T201～T275、T301～T318
- 当前场景：诊断首次调用创建持久化Session，后续可仅携带`session_id`继承当前订单或任务；每轮追加Message和Run、保存上一轮意图与最近诊断Run，并继续通过M3.1页面Schema和Java事实重校验
- 通过测试：真实跨服务E2E 8/8；Python汇总248通过/31个需外部环境的用例按门禁跳过；隔离PostgreSQL持久化与API 30/30；Web 7个测试文件16/16及生产构建通过；Ruff和mypy strict（61个源文件）通过；Compose配置校验通过
- 失败测试：最终结果0；开发中发现并修正诊断响应、前端运行时校验和迁移元数据对`session_id`及会话字段的契约联动，已完成回归
- 当前阻塞：无
- 开发环境：OpenJDK 21.0.12、Maven 3.9.16、Python 3.12.13（uv 管理）、uv 0.12.0、Node.js 22.22.2、npm 10.9.7、Docker Desktop 29.6.2
- 最近更新：M3.2增加严格`SessionContext`、会话创建/读取/清除API、用户所有权校验、可配置滑动TTL、诊断上下文继承、消息续号和最近Run保存；前端同一订单复用会话ID
- 已知非阻塞问题：当前会话只被固定订单诊断消费，尚无M3.3意图路由和自然语言补参；TTL目前只阻止继续访问，尚无后台物理清理；首次诊断若在成功响应前失败，调用方拿不到该轮新建的`session_id`；清除会话会级联删除其Agent Message/Run/Step，尚无归档策略；当前只有订单业务页实际接入Adapter，任务与质检页面尚未建设；`batch_id`和`satellite_type`没有Java事实可供重校验；演示身份Header不是完整认证；诊断仍为单次HTTP响应，没有SSE，建议不能确认执行；未预期节点异常可能遗留RUNNING Step，尚无崩溃恢复；具体模型客户端、Prompt版本和真实模型评测未装配；没有Step重试次数/Trace持久化或跨实例调用账本；调试Run与持久化Run仍未关联；本地Java/Python数据库角色未隔离；Java测试仍有Mockito动态Agent的未来JDK兼容警告
- 下一任务：T319～T325 实现 M3.3 意图枚举、输入输出Schema和规则基线
