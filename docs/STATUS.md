# 当前开发状态

- 当前里程碑：M0 业务数据与 Java 接口
- 当前子阶段：M0.6 Java 统一异常（已完成）
- 已完成任务：T001～T044
- 当前场景：全部 `/api` 业务端点使用统一 `ApiResponse<T>`；400/401/403/404/409/500 映射为稳定错误码，Trace ID 可安全透传或生成，并同步到响应 Header、响应体和日志 MDC
- 通过测试：Maven 全量测试 48/48；M0.6 根级 `make test-java-errors` 8/8；M0.4 查询契约 13/13、M0.5 写接口 12/12、M0.2 回归 7/7、M0.3 数据测试 7/7；`make test` 的基础检查和三服务冒烟通过
- 失败测试：最终结果 0；开发中测试先行联合运行 31 个用例时 16 个失败、15 个通过，失败来自统一信封、错误码、Trace ID 和 401 语义尚未实现；完成实现并补充边界用例后，联合回归 33/33 通过
- 当前阻塞：无
- 开发环境：OpenJDK 21.0.12、Maven 3.9.16；Homebrew 安装 Maven 时另安装 OpenJDK 26.0.2 作为其依赖，但项目和 Shell 继续固定使用 JDK 21
- 最近治理更新：DOC-003 已建立 Agent 面试价值门禁；每次开发先评估，只有确有面试价值时才更新 `doc/needCare.md`
- 已知非阻塞问题：`X-User-Id`/`X-User-Role` 仍是尚未连接真实认证的阶段性身份上下文；通用 500 保守标记为不可自动重试，后续需按 M0.7 的明确故障类型再区分安全可重试读异常；Trace ID 尚未串联 Python Run/Step；幂等记录尚无过期/归档策略
- 下一任务：T045 实现延迟故障模拟
