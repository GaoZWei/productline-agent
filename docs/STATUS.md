# 当前开发状态

- 当前里程碑：M4 RAG规范检索（进行中）
- 当前子阶段：M4.11规范问答 Workflow（T469～T476，已完成）
- 已完成任务：T001～T153、T201～T275、T301～T354、T401～T476
- 当前场景：`SPEC_QA`可通过`SpecificationSkill`进入固定RAG图，确定性执行查询规范化、页面提示合并、关键词/向量召回、RRF、Rerank和带引用生成；引用保留规范名称、版本、章节、全部Chunk身份和原文，无结果、重排超时或模型引用异常不会形成规范结论
- 通过测试：真实跨服务E2E最近一次8/8；Python汇总388通过/38个需外部环境的用例按门禁跳过；M4知识测试82/82；M4.10独立验收后端18/18、前端2/2；M4.11独立验收12/12；真实隔离PostgreSQL关键词、向量和混合检索透传3/3，持久化、迁移与API最近一次37/37；Web 8个测试文件18/18及生产构建通过；Ruff和mypy strict（102个源文件）通过；Compose配置校验通过
- 失败测试：最终结果0；开发中先以缺少`build_citations`、引用卡片和`KnowledgeRetrievalPipeline`导出验证新测试有效；质量检查发现新导出排序及既有解释性注释标点不符合Ruff规则，修正格式后全量通过，未弱化断言
- 当前阻塞：无
- 开发环境：OpenJDK 21.0.12、Maven 3.9.16、Python 3.12.13（uv 管理）、uv 0.12.0、Node.js 22.22.2、npm 10.9.7、Docker Desktop 29.6.2
- 最近更新：M4.10新增严格Citation、文档标题/版本检索透传和可展开前端引用卡片；M4.11新增`KnowledgeRetrievalPipeline`、固定`SpecificationQaWorkflow`和`SpecificationSkill`，只允许模型从既有候选选择引用，三类不可信路径返回稳定安全状态
- 已知非阻塞问题：规范问答仍是内部组件，尚未接入统一路由HTTP、页面问答交互、Run/Step持久化或具体问答/Rerank模型客户端；当前Query Rewrite只做NFKC和空白规范化，不处理别名、缩写或多轮指代；页面产品/卫星元数据只作为收窄提示且未由Java事实重校验，权限和生效日期仍必须显式提供；回答模型引用ID经过白名单校验，但尚无自动事实一致性或声明级引用覆盖评测；默认低相关阈值0.5和RRF`k=60`尚无真实语料评测校准；当前权限枚举只有`INTERNAL_REVIEWER`，尚无第二权限范围数据验证跨权限隔离；当前检索只返回`ACTIVE`规范，不支持历史审计as-of查询；M4.5双字词元可能产生词义无关共同双字召回，M4.6 HNSW尚无大规模召回率和延迟评测；尚未提供全目录入库CLI、定时任务或HTTP管理入口；测试使用固定向量与模型替身，没有声明真实语义或回答质量；当前token数是不绑定供应商的确定性近似值，重复检测只判断规范化正文完全相同，第一版不支持PDF；路由组件仍无FastAPI统一入口、持久化路由Run/Step或真实自然语言分发；TTL无后台物理清理，Session无归档策略；任务与质检页面尚未建设，`batch_id`和`satellite_type`缺少Java事实重校验契约；演示身份Header不是完整认证；诊断没有SSE或Approval，未预期节点异常可能遗留RUNNING Step；没有Step重试次数/Trace持久化或跨实例调用账本；本地Java/Python数据库角色未隔离；Java测试仍有Mockito动态Agent的未来JDK兼容警告
- 下一任务：T477～T487 实现M4.12固定RAG评测集、Hit@5、MRR、无关片段占比、四种检索策略对比和失败样本输出
