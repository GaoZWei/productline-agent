# 当前开发状态

- 当前里程碑：M0 业务数据与 Java 接口
- 当前子阶段：M0.5 Java 写接口（已完成）
- 已完成任务：T001～T037
- 当前场景：Java 服务除 8 个只读接口外，已支持提交复核结果和创建返工任务；写入前校验 `REVIEWER` 角色、任务/问题状态、幂等键和任务版本，成功后记录操作前后状态
- 通过测试：Maven 全量测试 40/40；M0.5 根级 `make test-java-write` 12/12；M0.4 查询契约 13/13、M0.2 回归 7/7、M0.3 数据测试 7/7；`make test` 的基础检查和三服务冒烟通过
- 失败测试：最终结果 0；开发中测试先行 11 个写接口测试因表/接口未实现全部报错，随后修复迁移类型映射、JPA 级联重复持久化、事务提交阶段版本冲突和测试夹具污染，最终扩充为 12/12 通过
- 当前阻塞：无
- 开发环境：OpenJDK 21.0.12、Maven 3.9.16；Homebrew 安装 Maven 时另安装 OpenJDK 26.0.2 作为其依赖，但项目和 Shell 继续固定使用 JDK 21
- 最近治理更新：DOC-003 已建立 Agent 面试价值门禁；每次开发先评估，只有确有面试价值时才更新 `doc/needCare.md`
- 已知非阻塞问题：统一错误体、错误码和 Trace ID 属于 M0.6；`X-User-Id`/`X-User-Role` 是尚未连接真实认证的阶段性身份上下文；幂等记录尚无过期/归档策略；总览接口的生产规模查询优化仍需指标驱动
- 下一任务：T038 定义统一 `ApiResponse<T>` 响应结构
