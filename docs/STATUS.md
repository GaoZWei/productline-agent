# 当前开发状态

- 当前里程碑：M4 RAG规范检索（进行中）
- 当前子阶段：M4.5关键词检索与M4.6向量检索（T431～T441，已完成）
- 已完成任务：T001～T153、T201～T275、T301～T354、T401～T441
- 当前场景：入库时将章节、原文和中文双字词元写入GIN全文索引，关键词查询返回归一化cover-density分数；Query Embedding复用文档Provider契约，向量查询仅在相同Provider、模型、维度和索引版本内按HNSW余弦距离、TopK和阈值召回
- 通过测试：真实跨服务E2E最近一次8/8；Python汇总345通过/35个需外部环境的用例按门禁跳过；M4知识测试39/39；M4.5验收17个单元/契约测试和1个隔离PostgreSQL测试；M4.6验收20个单元/契约测试和2个隔离PostgreSQL测试；隔离PostgreSQL持久化、迁移与API 34/34；Web最近一次7个测试文件16/16及生产构建通过；Ruff和mypy strict（92个源文件）通过；Compose配置校验通过
- 失败测试：最终结果0；开发中先以缺少关键词契约验证测试有效，随后真实PostgreSQL门禁暴露并修正`search_document`必填与`regconfig`参数类型问题；质量检查同时修正重新索引删除条件误用ORM对象的问题
- 当前阻塞：无
- 开发环境：OpenJDK 21.0.12、Maven 3.9.16、Python 3.12.13（uv 管理）、uv 0.12.0、Node.js 22.22.2、npm 10.9.7、Docker Desktop 29.6.2
- 最近更新：M4.5增加章节/正文检索文档、NFKC中文双字预处理、GIN全文索引、`plainto_tsquery`匹配和关键词分数；M4.6增加Query Embedding、零向量门禁、HNSW余弦索引、索引身份隔离、TopK、阈值和向量分数，并提供两个独立验收入口
- 已知非阻塞问题：M4.5双字词元是无外部分词依赖的确定性第一版，可能产生词义无关的共同双字召回；M4.6 HNSW是近似索引，尚无大规模召回率和延迟评测；M4.7尚未实现产品、卫星、文档类型、版本、生效时间和权限过滤，因此当前Repository不能直接用于生成规范结论；尚无混合检索、相邻片段合并、引用组装或RAG API；尚未提供全目录入库CLI、定时任务或HTTP管理入口；测试使用MockTransport和固定向量，没有调用外部Provider或声明真实语义质量；当前token数仍是不绑定供应商的确定性近似值，尚未用实际模型tokenizer校验批次token上限；重复检测只判断规范化后正文完全相同，既有数据库哈希需由调用方传入；第一版不支持PDF；当前评测只用可控Subject验证路由基础设施，尚无具体诊断模型供应商和真实模型指标；“高分二号”到`GF-2`别名样本会暴露现有字面证据策略缺口，未在无真实模型对比前放宽安全校验；`IntentRouter`、实体合并器和决策器仍是分离的内部组件，尚无FastAPI统一入口、持久化路由Run/Step或真实自然语言分发；模型调用异常直接回退，当前只对结构或实体证据失败重试一次；TTL目前只阻止继续访问，尚无后台物理清理；首次诊断若在成功响应前失败，调用方拿不到该轮新建的`session_id`；清除会话会级联删除其Agent Message/Run/Step，尚无归档策略；当前只有订单业务页实际接入Adapter，任务与质检页面尚未建设；`batch_id`和`satellite_type`没有Java事实可供重校验；演示身份Header不是完整认证；诊断仍为单次HTTP响应，没有SSE，建议不能确认执行；未预期节点异常可能遗留RUNNING Step，尚无崩溃恢复；没有Step重试次数/Trace持久化或跨实例调用账本；调试Run与持久化Run仍未关联；本地Java/Python数据库角色未隔离；Java测试仍有Mockito动态Agent的未来JDK兼容警告
- 下一任务：T442～T449 实现M4.7产品、卫星、文档类型、版本、生效时间和权限过滤，以及跨产品与历史规范误召回测试
