# 当前开发状态

- 当前里程碑：M4 RAG规范检索（进行中）
- 当前子阶段：M4.9 Rerank（T457～T461，已完成）
- 已完成任务：T001～T153、T201～T275、T301～T354、T401～T461
- 当前场景：混合候选可通过供应商无关Reranker按0～1相关性稳定重排并拦截低分片段，原始RRF证据保持不变；模型超时显式标记`TIMEOUT`并回退原顺序，未知、重复、缺失或非法分数关闭失败
- 通过测试：真实跨服务E2E最近一次8/8；Python汇总376通过/38个需外部环境的用例按门禁跳过；M4知识测试70/70；M4.9直接单元测试18/18，独立验收入口连同M4.8回归30/30；隔离PostgreSQL持久化、迁移与API最近一次37/37；Web最近一次7个测试文件16/16及生产构建通过；Ruff和mypy strict（96个源文件）通过；Compose配置校验通过
- 失败测试：最终结果0；开发中先以缺少`RerankExecutionError`导出验证新契约测试有效，首次实现的严格Tuple字段拒绝常见对象数组，随后仅放宽集合容器转换并继续保持字段类型、额外字段和分数范围严格校验
- 当前阻塞：无
- 开发环境：OpenJDK 21.0.12、Maven 3.9.16、Python 3.12.13（uv 管理）、uv 0.12.0、Node.js 22.22.2、npm 10.9.7、Docker Desktop 29.6.2
- 最近更新：M4.9新增严格`Reranker`协议和`RerankOutcome`，模型只为既有候选打分且不能新增或漏掉身份；正常路径按分数稳定重排并执行阈值门禁，超时路径保留原RRF顺序、原始证据和显式降级原因
- 已知非阻塞问题：当前只有供应商无关Reranker接口和可控测试替身，尚未接入具体模型客户端、统一RAG Workflow或HTTP API；默认低相关阈值0.5尚无真实语料评测校准，超时降级因没有可信模型分数会保留全部原召回候选；尚无引用组装；当前RRF固定`k=60`且尚无真实语料评测验证该参数；融合函数只处理调用方提供的候选列表，不负责查询生成、候选池扩展或并发召回；相邻片段只合并两路候选中已经命中的同文档同章节连续Chunk，不主动从数据库补取未召回邻居；当前权限枚举只有`INTERNAL_REVIEWER`，已实现fail-closed精确过滤但尚无第二权限范围数据可做跨权限隔离测试；当前检索只返回`ACTIVE`规范，不支持面向历史审计的as-of版本查询；M4.5双字词元是无外部分词依赖的确定性第一版，可能产生词义无关的共同双字召回；M4.6 HNSW是近似索引，尚无大规模召回率和延迟评测；尚未提供全目录入库CLI、定时任务或HTTP管理入口；测试使用MockTransport和固定向量，没有调用外部Provider或声明真实语义质量；当前token数仍是不绑定供应商的确定性近似值，尚未用实际模型tokenizer校验批次token上限；重复检测只判断规范化后正文完全相同，既有数据库哈希需由调用方传入；第一版不支持PDF；当前评测只用可控Subject验证路由基础设施，尚无具体诊断模型供应商和真实模型指标；“高分二号”到`GF-2`别名样本会暴露现有字面证据策略缺口，未在无真实模型对比前放宽安全校验；`IntentRouter`、实体合并器和决策器仍是分离的内部组件，尚无FastAPI统一入口、持久化路由Run/Step或真实自然语言分发；模型调用异常直接回退，当前只对结构或实体证据失败重试一次；TTL目前只阻止继续访问，尚无后台物理清理；首次诊断若在成功响应前失败，调用方拿不到该轮新建的`session_id`；清除会话会级联删除其Agent Message/Run/Step，尚无归档策略；当前只有订单业务页实际接入Adapter，任务与质检页面尚未建设；`batch_id`和`satellite_type`没有Java事实可供重校验；演示身份Header不是完整认证；诊断仍为单次HTTP响应，没有SSE，建议不能确认执行；未预期节点异常可能遗留RUNNING Step，尚无崩溃恢复；没有Step重试次数/Trace持久化或跨实例调用账本；调试Run与持久化Run仍未关联；本地Java/Python数据库角色未隔离；Java测试仍有Mockito动态Agent的未来JDK兼容警告
- 下一任务：T462～T467 实现M4.10 Citation Schema、文档名与版本、章节路径、Chunk身份、相关性分数和前端引用卡片
