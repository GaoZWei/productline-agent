# 当前开发状态

- 当前里程碑：M5 动态异常诊断Agent（进行中）
- 当前子阶段：M5.5 信息充分度判断（T532～T538，已完成）
- 已完成任务：T001～T153、T201～T275、T301～T354、T401～T487、T501～T538
- 当前场景：固定订单诊断保持原有全量事实路径；动态诊断在初始化及每次Observation后确定性计算基础、生产、质检、复核、交付和规范缺口，缺口进入动作Prompt和结束判断，执行预算、重复、风险、参数、资源及权限门禁继续生效
- 通过测试：Python汇总428通过/38个需外部环境的用例按门禁跳过；M5.5基础、嵌套归属、四类业务场景、规范缺口和动态规则7/7，M5.3～M5.5相关回归44/44；mypy strict（114个源文件）通过；本次6个目标文件Ruff和格式检查通过；此前真实跨服务E2E最近一次8/8、M4知识与RAG测试91/91、Web 18/18及生产构建、Compose健康检查与配置校验结果保持有效
- 失败测试：开发中先以缺少`InformationGapDetector`公开入口验证新测试有效；首次动态回归仍期望缺规范时输出质量结论而失败，按M5.5边界修正为信息不足并断言稳定缺口后通过；全量Ruff仍被5个既有文件的42项注释标点/行长问题拦截，全量格式检查仍有71个既有文件待格式化
- 当前阻塞：无
- 开发环境：OpenJDK 21.0.12、Maven 3.9.16、Python 3.12.13（uv 管理）、uv 0.12.0、Node.js 22.22.2、npm 10.9.7、Docker Desktop 29.6.2
- 最近更新：M5.5新增`InformationGapDetector`，区分未查询与已知为空，校验嵌套事实归属并按场景重算缺口；动态`FINISH`和预算终止只有缺口清空时才形成结论，固定Workflow保持M2.6全量规则
- 已知非阻塞问题：M5.4尚无具体动作模型供应商；默认6轮不足以完成ORDER-003所需的订单、任务、质检、复核、交付、规范检索及FINISH全链路，M5.6路径测试需显式配置充足轮数；8次Tool预算当前通常不可达，但作为独立外部调用上限保留；规范问答的安全无结论响应视为已完成检索尝试但不会形成规范结论，后续仍需评估是否增加可引用规范的独立缺口状态；全量Ruff存在5个既有文件的42项注释标点/行长问题，全量格式检查存在71个既有未格式化文件；M4.12可控Subject用于验证数据契约、策略执行和指标数学，不代表尚未接入的真实Embedding/Reranker质量，默认低相关阈值0.5和RRF`k=60`仍需真实Provider评测校准；规范问答仍是内部组件，尚未接入统一路由HTTP、页面问答交互、Run/Step持久化或具体问答/Rerank模型客户端；当前Query Rewrite只做NFKC和空白规范化，不处理别名、缩写或多轮指代；页面产品/卫星元数据只作为收窄提示且未由Java事实重校验，权限和生效日期仍必须显式提供；回答模型引用ID经过白名单校验，但尚无自动事实一致性或声明级引用覆盖评测；当前权限枚举只有`INTERNAL_REVIEWER`，50条评测均为DOM/GF-2/内部复核范围，尚无第二产品、卫星或权限范围的标注对比；当前检索只返回`ACTIVE`规范，不支持历史审计as-of查询；M4.5双字词元可能产生词义无关共同双字召回，M4.6 HNSW尚无大规模召回率和延迟评测；尚未提供全目录入库CLI、定时任务或HTTP管理入口；当前token数是不绑定供应商的确定性近似值，重复检测只判断规范化正文完全相同，第一版不支持PDF；路由组件仍无FastAPI统一入口、持久化路由Run/Step或真实自然语言分发；TTL无后台物理清理，Session无归档策略；任务与质检页面尚未建设，`batch_id`和`satellite_type`缺少Java事实重校验契约；演示身份Header不是完整认证；诊断没有SSE或Approval，未预期节点异常可能遗留RUNNING Step；没有Step重试次数/Trace持久化或跨实例调用账本；本地Java/Python数据库角色未隔离；Java测试仍有Mockito动态Agent的未来JDK兼容警告
- 下一任务：T539～T549 实现M5.6五个固定订单动态路径及边界回归测试
