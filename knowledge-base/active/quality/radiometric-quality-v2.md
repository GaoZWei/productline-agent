# DOM辐射质量检查规范

> 演示规范数据，非真实行业标准。

## 检查范围

辐射质量检查覆盖影像异常、色调一致性和无效像元记录。
检查必须基于当前成果版本，不能从页面展示状态直接判定。

## 证据要求

每项异常应关联检查字段、实际值和可定位的问题记录。
仅有自然语言描述而没有业务记录时，应标记为信息不足。

## 通过条件

所有辐射质量问题关闭且复核通过后，本项检查才能视为完成。
生产状态COMPLETED不能替代质量检查和复核状态。

## 重新检查

涉及影像内容的返工完成后必须重新检查，旧结论不得直接沿用。

