# DOM云量检查规范

> 演示规范数据，非真实行业标准。

## 检查对象

云量检查面向DOM成果覆盖范围及其质量报告。
检查输入必须对应当前任务版本，不能复用其他订单的结论。

## 检查记录

检查结果应记录检测范围、问题描述和检查时间。
无法确认云覆盖影响时，应形成待处理质量问题而不是自动判定通过。

## 通过条件

质量报告满足项目约定且没有相关未关闭问题时，云量检查才可通过。
通过状态必须由Java业务事实确认，模型只能解释检查依据。

## 复核要求

云量问题处理完成后应重新提交复核，原有PENDING结果不能视为通过。

