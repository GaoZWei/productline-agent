# DOM成果元数据规范

> 演示规范数据，非真实行业标准。

## 必填标识

每份DOM成果必须包含订单号、任务号、产品类型和规范版本。
业务标识必须能够追溯到Java业务服务中的最新记录。

## 空间信息

元数据应声明坐标参考、处理级别和成果覆盖范围。
坐标参考缺失时不得以文件名或页面参数推断最终值。

## 质量信息

元数据应关联质量问题和最近一次复核结果。
存在未关闭质量问题时，元数据状态必须反映成果仍不可交付。

## 一致性检查

清单、成果文件和数据库记录中的订单号及任务号必须一致。

