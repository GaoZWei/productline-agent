# Agent 面试答案

本文件用于沉淀可直接口述的答案。每题优先准备 30 秒版和完整回答，再记录具体案例、记忆框架与事实边界。

## Q01｜为什么订单诊断需要 Agent？直接做一个聚合查询页面不够吗？

### 30 秒回答

并不是所有订单问题都需要 Agent。如果只是查询订单状态或生产进度，聚合页面和确定性流程已经足够。

Agent 主要解决“为什么订单还没有交付”这类需要综合订单、生产、质检、复核和交付等多个业务模块进行取证的问题。不同订单的阻塞位置不同，Agent 可以根据已有事实动态选择下一步只读查询。

在我的项目中，模型只负责理解意图和选择取证路径，业务事实来自 Java Tool，最终诊断由确定性规则生成，写操作还需要人工确认。

### 完整回答

这个项目最开始要解决的，不只是查询订单状态，而是为订单、生产、质检、复核和交付等业务环节提供一个统一的自然语言入口。

用户在同一个订单页面中可能提出不同类型的问题。例如，他可能询问“这个订单生产到哪一步了”，也可能问“为什么还不能交付”“坐标系问题应该怎样处理”，或者要求系统“根据当前诊断结果整理一份复核意见”。这些问题依赖的业务数据和处理路径不同。

如果只是查询固定字段，聚合页面或普通 Workflow 更合适。因此，项目中的状态查询不会使用动态 Agent，而是按照固定流程查询订单和生产任务，直接返回结构化结果。

Agent 主要用于复杂诊断。比如用户询问订单为什么不能交付时，系统无法仅凭订单状态得出结论。它需要根据当前已经取得的事实，判断下一步应该查询生产任务、质检问题、复核状态还是交付条件。不同订单的阻塞位置不同，实际需要的取证路径也可能不同，所以这里由模型选择下一步只读 Tool。

以 `ORDER-003` 为例，它的生产任务已经完成，但还存在未关闭的坐标系问题，复核状态也是待处理。因此，系统需要继续读取质检和复核事实，才能判断当前阻塞在质量复核阶段，而不是生产阶段。

模型在这里负责理解用户意图和选择取证路径，但不会生成业务事实，也不会直接决定最终业务状态。订单、任务和质检问题之间的关系以及当前状态，都由 Java Tool 提供；当事实足够后，由确定性规则生成阻塞环节、事实依据和处理建议。

对于规范类问题，系统进入带版本和权限过滤的 RAG 流程；对于复核提交和返工创建等写操作，模型只能生成草稿或建议，最终必须经过人工确认，并由 Java 服务重新校验权限、状态和版本。

所以，我在这个项目中采用的不是“所有问题都交给模型”的方式，而是单主 Agent 加多个受约束业务 Skill：模型负责意图理解和动态取证，Workflow 负责稳定流程，Java 服务负责业务事实和最终写入。这样既保留了自然语言交互和动态决策能力，也控制了模型幻觉和误操作风险。

### 具体案例：ORDER-003

```text
ORDER-003
→ TASK-003：production_status = COMPLETED
→ ISSUE-001：type = COORDINATE_SYSTEM，status = OPEN
→ review_status = PENDING
→ delivery_status = BLOCKED
→ blocking_stage = QUALITY_REVIEW
```

诊断结论：生产已经完成，但未关闭的坐标系问题和待复核状态仍在阻塞交付。

处理建议：创建坐标系返工任务，问题处理完成后重新提交复核。

### 记忆框架

1. **简单查询不需要 Agent**：聚合页面和固定流程更合适。
2. **复杂诊断需要动态取证**：综合生产、质检、复核和交付等业务模块。
3. **用 ORDER-003 说明区别**：生产完成不等于可以交付。
4. **明确模型边界**：模型选择路径，Java 提供事实，规则生成结论，人工确认写入。

### 核心总结

> Agent 的价值不是替代聚合页面，而是在多业务模块、取证路径不固定的场景中，根据已有事实动态选择下一步查询，同时保持业务事实和最终写入的确定性边界。

### 追问 1｜既然最终诊断由规则生成，为什么不全部使用固定 Workflow？

固定 Workflow 当然可以实现。如果问题类型固定、Tool 数量较少，而且每次需要查询的数据相同，确定性流程会更简单、更稳定。

项目引入 Agent，主要是为了处理取证路径的不确定性，而不是代替最终业务规则。用户的问题不同，当前已经掌握的事实也不同，因此每次需要查询的数据不一定相同。

例如，生产任务还没有完成时，系统需要继续查询生产步骤，定位具体阻塞环节；生产已经完成时，就没有必要继续查询生产步骤，而应该转向质检问题、复核状态和交付条件。如果用户询问的是产品规范，则应该进入 RAG 流程，而不是继续调用业务状态 Tool。

固定 Workflow 也可以通过条件分支覆盖这些路径，但随着问题类型、业务状态和 Tool 数量增加，分支会越来越多。Agent 可以根据用户意图、已有事实和当前信息缺口选择下一步只读动作，减少无关调用，也便于后续增加新的查询能力。

不过，模型的决策范围受到确定性边界约束。它只能从已注册的只读 Tool 中选择动作，执行前还要经过参数、权限、资源归属和调用预算校验；事实收集完成后，最终阻塞原因仍然由确定性规则生成。

因此，项目采用的是混合方案：简单、稳定的路径使用固定 Workflow；复杂诊断的取证阶段使用 Agent；业务事实判断和写入继续保持确定性。

是否值得保留动态 Agent，还需要与固定流程进行对照评测，比较证据覆盖率、错误 Tool 调用率、平均 Tool 数、耗时和成本。如果动态方案没有带来足够收益，就应该缩小它的使用范围。

> Agent 负责决定还需要查什么，规则负责根据已确认的事实得出什么结论。

### 追问 2｜为什么不让模型直接生成最终诊断？

我没有让模型直接生成最终诊断，因为诊断结果会影响后续复核和返工，需要保证结果稳定、可追溯、可测试。Structured Output 只能保证返回格式正确，不能保证业务结论正确。

在项目中，模型负责理解问题并选择下一步只读 Tool，Java 服务提供可信业务事实，确定性规则根据事实生成阻塞阶段、根因和建议。比如 `ORDER-003` 的生产已经完成，但存在未关闭的坐标系问题且复核待处理，规则会稳定判断它阻塞在质量复核阶段。

模型可以负责组织解释和生成草稿，但不能覆盖规则结论。遇到规则未覆盖的状态组合，系统返回证据不足，而不是让模型猜测。

> 模型处理语言和路径的不确定性，规则保证业务结论的确定性。

### 追问 3｜怎样证明动态 Agent 比固定 Workflow 更有价值？

当前的状态查询 Workflow 和动态诊断 Agent 解决的问题不同，不能直接比较。前者回答“订单现在是什么状态”，后者回答“为什么处于这个状态”。

如果要评测，我会增加一个合理的固定诊断 Workflow，让它和动态 Agent 接收相同的诊断问题，使用相同的 Java Tool、业务数据和最终诊断规则。固定 Workflow 也可以使用正常的条件分支，不能故意设计成每次查询全部数据。两者唯一的变量是：取证路径由代码分支决定，还是由模型根据已有事实和信息缺口动态决定。

评测时先比较诊断正确率、证据覆盖率和异常情况下的安全停止，确保 Agent 不会漏掉关键事实；再比较 Tool 调用数、重复或错误调用、端到端耗时和模型成本。

如果 Agent 在保持诊断正确的前提下减少了无关查询，或者能在现有 Tool 和诊断规则支持的范围内适应新的事实组合，就体现了动态取证的价值。如果正确性、耗时和成本没有明显收益，就应该继续使用固定 Workflow。

当前项目已经具备相关指标和评测框架，但固定诊断基线与真实模型的完整对照评测仍需补充。

> 保持输入、Tool 和诊断规则相同，只比较取证路径由代码决定，还是由模型动态决定。

### 当前边界

- 当前项目实现的是多业务模块联合诊断，不描述为已经完成多个独立系统之间的集成。
- 固定状态查询不使用动态 Agent，避免增加不必要的模型成本和不确定性。
- 模型只选择受限的只读动作，不能修改 Java 业务事实或直接执行写操作。
- 当前受控调用链和确定性规则已有实现与测试；真实模型的泛化效果、耗时和成本仍需结合真实评测说明。

## Q04｜从用户输入到返工创建，完整请求链怎么走？

### 30 秒回答

用户在订单页面输入问题后，请求进入统一 Agent 接口。系统为本轮交互创建 Run 并识别意图；如果是订单诊断，Agent 会调用受限的只读 Java Tool 收集业务事实，必要时查询规范，最后由确定性规则生成诊断结果。

用户要求整理复核意见时，系统会基于同一 Session 中最近一次成功诊断，重新读取最新业务事实和规范依据，生成复核草稿和待确认 Approval。用户确认后，系统再次校验权限、状态和版本，再由 Java 写入复核结果。

如果复核结论要求返工，用户还需要单独发起返工请求并再次确认，Java 才会创建返工任务。复核提交和返工创建是两个独立写操作，不能共用一次授权。

### 完整回答

用户在订单页面输入“为什么这个订单还不能交付”后，前端会把消息、Session 和当前页面上下文发送到统一 Agent 接口。页面上下文只能帮助定位查询对象，不能直接作为可信业务事实。

Agent Service 会为本轮消息创建 Turn 和 Run，然后识别用户意图。诊断请求进入动态诊断流程：模型根据已有信息选择下一步只读 Tool，Java 服务返回订单、生产任务、质检问题、复核状态和交付状态等业务事实；必要时，流程还会通过 RAG 查询适用的规范。事实收集完成后，由确定性规则生成阻塞阶段、根因、事实依据和处理建议，模型不能补造业务状态或直接执行写操作。

以 `ORDER-003` 为例，Java Tool 返回它的生产任务已经完成，但坐标系问题仍未关闭，复核状态为待处理，交付状态为阻塞。因此，规则会判断订单阻塞在 `QUALITY_REVIEW`，而不是生产阶段。

如果用户接着要求“整理复核意见”，系统会创建一个新的 Run，并关联同一 Session 中最近一次成功诊断。复核流程会重新读取当前任务和质检问题，再检索当前有效的规范及引用，然后由模型生成结构化复核草稿。草稿包括复核结论、问题摘要、复核意见、规范引用和返工建议，保存后形成状态为 `WAITING_CONFIRMATION` 的 Approval。

用户可以检查或修改草稿。确认时，系统会重新校验用户身份和权限，并刷新任务状态、质检问题和任务版本。校验通过后，Approval 才进入执行状态，并通过 Java 写 Tool 提交复核结果；如果事实或版本已经变化，原 Approval 会失效，避免按照过期草稿写入。

如果最终复核结论明确要求返工，系统也不会自动创建返工任务。用户必须显式发起返工请求，系统根据已经成功提交的复核结果创建新的 Run 和第二个 Approval。用户再次确认后，系统再次校验最新事实和版本，最后由 Java 创建状态为 `PENDING` 的返工任务。

因此，这条链路不是一次请求自动完成所有操作，而是多轮交互和两次独立授权：

```text
用户问题
→ 意图路由
→ 只读取证
→ 确定性诊断
→ 生成复核草稿
→ Approval A：确认并提交复核
→ 用户显式请求返工
→ Approval B：确认并创建返工任务
```

创建返工任务只表示返工工作已经建立，不表示问题已经解决，也不表示订单已经可以交付。

### 具体案例：ORDER-003

```text
ORDER-003
→ TASK-003：production_status = COMPLETED
→ ISSUE-001：type = COORDINATE_SYSTEM，status = OPEN
→ review_status = PENDING
→ delivery_status = BLOCKED
→ blocking_stage = QUALITY_REVIEW
→ 生成复核草稿并等待第一次确认
→ 提交“需要返工”的复核结果
→ 创建独立返工 Approval 并等待第二次确认
→ 创建状态为 PENDING 的坐标系返工任务
```

### 记忆框架

1. **先路由**：统一入口识别状态查询、诊断、规范问答或复核请求。
2. **再取证**：模型选择只读路径，Java 提供业务事实，RAG 提供规范依据。
3. **规则诊断**：确定性规则生成阻塞阶段、根因和建议。
4. **两次授权**：提交复核和创建返工分别确认、分别写入。

### 核心总结

> 一次诊断、两次写入、两次授权：先通过只读取证形成诊断，再确认提交复核；确实需要返工时，再单独确认创建返工任务。

### 追问 1｜Session、Turn、Run、Step、Approval 分别是什么，什么时候创建？

Session 表示一段连续对话，用来管理用户归属和必要的会话上下文。Turn 表示用户和系统之间的一轮交互，但当前项目没有单独的 Turn 表，而是通过用户消息和执行记录来体现。

Run 表示一次可以独立追踪的执行任务。普通消息进入统一接口时会创建一个 Run；诊断、复核和返工属于不同执行任务，因此可以分别创建 Run，并通过 `source_run_id` 关联上下游。

Step 是 Run 内部的执行步骤，例如上下文加载、意图路由、模型决策、Tool 调用、RAG 检索和规则计算，用来记录执行过程、耗时和失败位置。

Approval 是与 Run 关联的写操作授权单，只在提交复核或创建返工等写操作前创建。它保存草稿、目标版本和待执行 Tool，用户确认后才允许写入。

所以它们的关系是：一个 Session 包含多轮交互和多个 Run，一个 Run 包含多个 Step；涉及写操作时，Run 还会关联 Approval。以 `ORDER-003` 为例，诊断、复核和返工分别有自己的 Run，复核和返工还各自需要一个 Approval。

需要注意，Turn 是交互概念，当前不是独立持久化实体；Approval 也不是普通 Step，而是独立保存的授权记录，只是 Run 中会使用 `APPROVAL` 和 `WRITEBACK` 类型的 Step 记录等待确认和写回过程。

> Session 管对话，Turn 表示一轮交流，Run 管一次执行，Step 记录执行过程，Approval 控制业务写入。

### 追问 2｜ORDER-003 为什么定位到 QUALITY_REVIEW，而不是生产阶段？

`ORDER-003` 定位到 `QUALITY_REVIEW`，因为系统会按照生产、质量问题处理、复核和交付的顺序，选择最早未满足的业务阶段。

Java Tool 返回它的生产任务和生产步骤都已经完成，因此可以排除 `PRODUCTION` 和 `PRODUCTION_BLOCKED`。继续检查时，系统发现 `ISSUE-001` 是坐标系问题，并且状态仍然是 `OPEN`，所以当前最早未完成的是质量问题处理，规则将阻塞阶段设为 `QUALITY_REVIEW`。

复核状态为 `PENDING`、交付状态为 `BLOCKED` 都是有效证据，但它们处于更下游。如果质量问题已经变成 `RESOLVED`，只是复核还没有通过，阻塞阶段才会变成 `REVIEW`。

这些状态都来自 Java Tool，模型只负责选择取证路径，最终阻塞阶段由确定性规则计算。

> 生产已经完成，但质量问题仍是 `OPEN`，因此最早阻塞点是 `QUALITY_REVIEW`；只有问题已解决、复核未通过时，才是 `REVIEW`。

### 追问 3｜诊断、复核和返工是在同一次请求、同一个 Run、同一次授权中完成的吗？

不在同一次请求、同一个 Run 或同一次授权中完成。当前实现把它们拆成三个相互关联的 Run。

第一个是诊断 Run，只调用只读 Tool，不需要 Approval。第二个是复核 Run，它通过 `source_run_id` 关联成功的诊断 Run，生成复核草稿和复核 Approval。一个 Approval 只绑定一个确定的写 Tool，因此这个 Approval 只能授权调用 `write_review_result`。

用户确认复核是一次新的 HTTP 请求，但会继续执行处于等待确认状态的复核 Run，不会新建 Run。只有复核成功，并且用户最终确认的结论明确要求返工后，用户才能显式发起返工请求。系统随后创建第三个 Run 和独立的返工 Approval，这个 Approval 只允许调用 `create_rework_task`。

这样拆分是因为提交复核和创建返工是两个不同的业务副作用：提交复核只记录复核结论和意见，创建返工则会新增实际工作任务并影响后续生产安排。用户同意“需要返工”，不等于已经授权系统立即创建返工任务。一个 Approval 会固定绑定操作类型、目标版本和唯一写 Tool，不能在用户确认后扩大操作范围。

返工还必须建立在已经成功提交的最终复核结果上。如果用户把复核结论和返工建议一致地修改为“不需要返工”，或者复核写入失败，系统都不能创建返工 Approval。复核写入成功后任务版本也会发生变化，因此返工需要基于新版本重新授权和校验，并保存独立的幂等与审计记录。

```text
诊断 Run：只读取证，无 Approval
  ↓ source_run_id
复核 Run：复核 Approval → 确认 → write_review_result
  ↓ source_run_id
返工 Run：返工 Approval → 确认 → create_rework_task
```

> 三个 Run 通过 `source_run_id` 形成来源链；一次授权只对应一个明确的业务副作用，因此复核和返工必须分别确认。

### 追问 4｜复核草稿根据什么生成？没有前置诊断能否直接生成？

当前复核草稿基于三部分信息生成。

第一部分是同一 Session 中最近一次成功诊断，它提供订单、阻塞阶段、根因、证据和建议，并建立复核 Run 与诊断 Run 之间的来源关系。

第二部分是最新业务事实。复核流程会重新调用 Java Tool 查询目标任务和质检问题，并验证任务确实属于诊断订单。诊断结果只是历史执行证据，不能代替当前业务状态。

第三部分是当前规范依据。系统重新执行带权限和生效时间过滤的 RAG，取得规范内容、版本和引用。

模型根据诊断、最新事实和规范依据生成结构化复核草稿，包括复核结论、问题摘要、复核意见、规范引用和返工建议。随后，确定性代码还会校验任务、问题、结论、返工建议和引用之间是否一致。校验通过后只生成待确认 Approval，不会直接写入业务系统。

当前实现要求同一 Session 中必须存在成功诊断。如果没有，复核流程会拒绝生成草稿。这个限制是当前架构的设计选择，并不代表复核在技术上必须依赖独立诊断。未来可以在用户直接请求复核时，先自动执行诊断，再创建复核 Run，但当前还没有实现自动串联。

生成草稿时会重新执行 RAG；用户确认时会刷新 Java 事实和版本，但当前不会再次执行 RAG。因此，规范引用反映的是草稿生成时的规范状态。

> 诊断提供可追溯的判断背景，Java Tool 提供最新事实，RAG 提供规范依据，模型只负责整理复核草稿。

### 追问 5｜草稿生成后，业务状态或任务版本发生变化怎么办？

用户点击确认后，系统不会立即写入，而是先保存用户确认的草稿，再强制刷新 Java 业务事实。

如果发现任务版本、任务状态或质检问题已经变化，当前 Approval 会进入 `STALE`，对应 Run 进入失败终态，并且不会调用写 Tool。`STALE` 是终态，原 Approval 不能退回等待确认状态，也不能再次使用。

当前系统不会自动修改旧草稿或自动创建新 Approval。用户需要基于最新事实重新进入诊断、复核或返工流程，生成新的草稿和 Approval，再重新确认。用户确认的是特定业务版本下的具体内容；事实变化后继续使用原授权，可能导致实际执行内容与用户看到的草稿不一致，因此必须废弃旧授权。

如果业务变化发生在 Agent 刷新事实之后、Java 真正写入之前，Java 还会通过 `expectedVersion` 和业务规则进行最后一次校验。发生冲突时，同样停止写入并将 Approval 标记为 `STALE`。

确认阶段刷新的是 Java 任务、质检问题和版本，不会重新执行 RAG。因此，它能发现业务状态变化，但当前不能发现草稿生成后发生的规范版本变化。

> 业务事实变化后，旧 Approval 进入 `STALE` 并终止写入；系统基于新事实生成新授权，再让用户确认。

### 追问 6｜用户重复点击确认，会不会重复写入？

正常情况下不会重复写入，系统没有把安全性建立在前端按钮防抖上，而是在服务端设置了三层保护。

第一层是 Approval 状态锁。用户确认后，系统通过条件更新把 Approval 从 `CONFIRMED` 改为 `EXECUTING`，只有一个并发请求能够成功取得执行权，其他请求会收到“正在执行”的冲突。

第二层是 Java 幂等控制。Agent 根据 `approval_id` 和待执行写 Tool 生成稳定幂等键。即使因为超时或网络重试再次调用 Java，同一个 Approval 也不会重复创建复核记录或返工任务。

第三层是结果重放。如果 Approval 已经成功，同一用户使用完全相同的草稿再次确认，系统直接返回之前保存的执行结果；如果用户或草稿不同，则拒绝复用这次授权。

```text
状态锁：只允许一个请求执行
幂等键：只允许一次业务写入
结果重放：重复请求返回同一结果
```

当前还有一个崩溃窗口：如果 Java 已经写入成功，但 Agent 在保存成功状态前崩溃，Java 幂等记录仍能防止再次落库，但 Approval 可能停留在 `EXECUTING`。当前还没有自动恢复这类中间状态的后台任务。

> Agent 状态锁防并发，Java 幂等键防重复落库，成功结果重放保证重试一致。

### 追问 7｜诊断结果包括什么？规范检索结果属于业务事实吗？

诊断结果采用固定结构，主要包括订单编号、阻塞阶段、诊断摘要、结构化根因、字段级证据、处理建议和置信度。

其中，业务证据只能来自 Java 只读 Tool。每条证据都会记录 Tool 名称、字段路径、字段值和说明。例如，`ORDER-003` 的生产任务为 `COMPLETED`、坐标系问题为 `OPEN`、复核为 `PENDING`，这些字段共同支撑 `QUALITY_REVIEW` 的判断。模型自己的文字不能直接成为业务事实。

规范检索结果不属于业务事实。Java Tool 回答“当前发生了什么”，RAG 回答“按照哪个版本的规范应该怎样处理”。规范内容不能覆盖 Java 返回的订单、任务或问题状态，也不能直接决定阻塞阶段。

当前坐标系诊断过程中可能执行 RAG，但规范结果与 Java 事实分开保存在 Workflow 状态中，最终 `DiagnosisResult` 不包含规范答案和引用。复核草稿阶段会重新执行 RAG，并要求草稿携带规范引用。

当前 `confidence` 是确定性的二值标记：事实充分时为 `1.0`，信息不足时为 `0.0`，不是经过统计校准的模型置信概率。诊断的信息缺口检查目前只判断规范检索结果是否存在，还没有进一步要求检索状态成功且引用充分。因此，目前只能说明诊断流程执行了规范检索，不能声称最终诊断建议已经由规范引用完整支撑。

> Java Tool 提供业务事实并决定阻塞阶段，RAG 提供独立的规范依据；当前诊断输出只展示业务证据，复核草稿才明确携带规范引用。

### 追问 8｜创建返工任务后，订单是否就可以交付？

不能。创建返工任务只表示系统建立了后续处理工作，不表示返工已经完成，也不表示订单可以交付。

当前 Java 写操作的职责范围很窄：创建一条状态为 `PENDING` 的返工任务，将它关联到原质检问题，更新生产任务版本，并记录幂等结果和操作日志。它不会顺带修改质检问题、复核结论或交付状态。

以 `ORDER-003` 为例，坐标系返工任务创建成功后，`ISSUE-001` 仍然没有关闭，复核也没有通过，交付状态仍然是 `BLOCKED`。后续还需要执行返工、将问题处理为 `RESOLVED`、重新提交复核，并重新检查交付条件。只有这些条件都满足后，订单才可能进入交付。

当前项目的实现范围结束于“经过人工确认并安全创建返工任务”。返工执行、质量问题状态流转和交付解锁还没有实现，因此不能描述为已经完成从诊断到最终交付的完整业务闭环。

```text
创建返工任务 ≠ 返工完成
返工完成 ≠ 自动通过复核
复核通过 ≠ 自动允许交付
```

> 创建返工只是安全地把诊断建议转化为待执行任务；问题处理、复核通过并重新校验交付条件后，订单才可能交付。

### 追问 9｜Java 已经写入成功，但 Agent 保存结果时失败怎么办？

如果 Java 已经写入成功，但 Agent 在保存成功状态前失败，业务数据不会重复写入，但两个服务会出现暂时的状态不一致。对 Agent 来说，此时无法直接判断 Java 没有执行，还是已经执行但响应没有被持久化。

Java 会在同一个本地事务中完成业务写入、幂等结果和操作日志，因此业务结果已经可靠提交。Agent 中的 Approval 则可能停留在 `EXECUTING`，重复点击确认只会得到“正在执行”，不会自动修复状态。

这时不能创建新的 Approval，因为新的 Approval 会生成新的幂等键，可能被 Java 识别为新的业务请求。正确的恢复方式是继续使用原 Approval 和原幂等键，重放完全相同的 Java 请求：如果原写入已经成功，Java 返回之前保存的结果；如果原请求没有提交，Java 会根据原授权和 `expectedVersion` 决定执行或拒绝。Agent 拿到结果后，再补写 `execution_result`、操作日志以及 Approval、Run、Step 的最终状态。

```text
扫描超时的 EXECUTING Approval
→ 使用原 Approval 和原幂等键重放
→ Java 返回历史结果或进行版本校验
→ Agent 补齐本地终态
```

当前已经实现 Java 幂等、业务写入事务和操作日志，能够防止重复业务写入；尚未实现扫描异常 `EXECUTING` 状态并自动重放、对账和补写 Agent 终态的恢复任务。

这个场景不一定需要引入分布式事务。当前更合适的方案是使用本地事务保证 Java 写入原子性，再通过稳定幂等键和后台对账实现最终一致性。

> Java 幂等解决“不能重复写”，后台对账解决“Agent 如何知道最终结果”；当前项目实现了前者，后者仍是待办。

### 追问 10｜页面上下文为什么只能作为提示，不能作为业务事实？

页面上下文只能作为查询提示，因为它来自客户端，可能过期或被修改。Schema 校验只能证明字段格式和页面层级正确，不能证明资源真实存在、归属关系正确，或者当前用户有权访问。

它的主要作用是解决“这个订单”“当前任务”等指代问题。Router 会把用户消息、已确认会话实体、页面上下文和会话候选值进行确定性合并，优先级依次是：

```text
用户本轮明确输入
→ 已确认的会话实体
→ 当前页面上下文
→ 会话候选值
```

如果用户消息中的订单与页面订单不同，系统优先采用用户本轮明确输入，同时保留冲突记录；如果同一优先级出现多个值而无法确定，则进入澄清，不能让模型猜测。

实体确定后，系统仍然要通过服务端权限门禁和 Java Tool 校验资源是否存在、任务是否属于订单，并重新查询当前业务状态。动态 Agent 只能查询 Java 已经确认属于目标订单的任务，最终诊断也只能使用 Java 返回的事实。

页面中的角色字段同样不能直接授予权限。Tool 调用使用服务端请求身份和权限上下文进行校验，不能因为页面声称用户是 Reviewer，就允许执行复核或写操作。

```text
页面上下文：帮助确定查询对象
实体合并规则：处理来源优先级和冲突
服务端与 Java：校验权限、归属和当前事实
```

当前项目使用演示 Header 传递用户身份和角色，还不是生产级认证。因此，可以说明已经实现身份上下文、Tool 权限和资源归属校验，不能声称已经完成生产级身份认证与防伪。

> 页面上下文负责减少澄清，确定性合并规则负责解决冲突，Java Tool 才负责确认业务事实；格式合法不代表资源可信。

### 当前边界

- 当前复核草稿依赖同一 Session 中最近一次成功诊断；没有前置诊断时不能直接生成草稿。
- 诊断发现坐标系问题时可以补充规范检索，但业务状态仍只来自 Java Tool。
- 复核提交当前只新增复核记录，不会自动关闭质检问题、改变交付状态或创建返工任务。
- 返工创建目前只覆盖坐标系返工类型；创建成功也不代表返工已经完成。
