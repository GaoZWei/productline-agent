# AGENTS.md

## 1. 范围、优先级与按需读取

本文件适用于整个仓库；更近目录的 `AGENTS.md` 可补充或覆盖局部规则。冲突优先级：

```text
用户当前明确要求
→ 更近目录的 AGENTS.md
→ 本文件
→ doc/detailed-plan.md 当前阶段
→ doc/plan.md
→ 现有实现与其他文档
```

开始任务时只读取完成当前工作所需的最小上下文：

1. 必读 `docs/STATUS.md`；
2. 只读取 `doc/detailed-plan.md` 当前 `Mx.y` 小节，不读取整份文件；
3. 读取目标模块 README、相关代码、测试和配置；
4. 需要确认架构、业务边界或里程碑时，才读取 `doc/plan.md` 相关章节；
5. 需要了解既有实现时，才读取 `doc/record.md` 对应里程碑；
6. 完成 Agent 面试价值评估或更新条目时，才读取 `doc/needCare.md` 用途说明和相关条目；
7. 里程碑状态变化时才读取并更新 `docs/ROADMAP.md`。

优先用 `rg` 定位标题、符号或关键词，再用小范围 `sed` 读取。除非用户要求全文整理或局部信息不足，
不得一次输出完整大型文档、完整测试日志或无关 Diff。不得因减少上下文而跳过当前任务所需事实。

发现重大业务歧义、文档冲突、跨越停止线或需要扩大范围时，说明影响并请求确认。

## 2. 核心架构与事实边界

- `web-console`：页面展示、上下文采集、Agent 交互和人工确认；页面参数只能作为提示。
- `agent-service`：Tool、Workflow/Agent、RAG、Approval、Run/Step 和 SSE；不得直接读写 Java 业务表。
- `business-service`：业务事实、权限、状态、一致性、幂等、审计和最终写入。

不可破坏的原则：

```text
业务事实只来自 Java Tool
规范结论只来自带版本和引用的 RAG
模型只负责路由、归纳、解释和草稿
写操作必须人工确认，并由 Java 重新校验
```

`ORDER-003` 黄金链路必须保持可重复、可重置、可自动测试：

```text
ORDER-003
→ TASK-003 (production_status = COMPLETED)
→ ISSUE-001 (type = COORDINATE_SYSTEM, status = OPEN)
→ review_status = PENDING
→ delivery_status = BLOCKED
→ blocking_stage = QUALITY_REVIEW
```

预期根因是“未关闭的坐标系问题 + 待复核”，建议是“创建坐标系返工任务 + 处理后重新提交复核”。
未经用户批准，不得修改固定数据、状态映射或让模型补造事实。

## 3. 开发顺序与实现约束

固定顺序：

```text
Java 接口可验证
→ Python HTTP Client/Tool 可验证
→ 确定性 Workflow 可验证
→ 动态 Agent 决策
```

一次只完成一个可测试的小任务，通常对应一个任务编号。达到当前里程碑停止线后先验收和汇报，
不得自动进入下一里程碑，也不得提前建设多 Agent、长期记忆、复杂 RBAC、工作流拖拽等计划外能力。

实现时必须遵守：

1. 保护用户已有改动，不做无关重构或依赖升级；
2. 先确认输入、输出、异常和失败测试，再做范围内最小实现；
3. 契约变化同步 Java、Python、前端、测试和相关文档；
4. 固定数据不得随机化，测试不得依赖执行顺序或外部不稳定状态；
5. Tool 使用明确 Schema，区分参数、资源、权限、超时、上游和响应校验错误；
6. 只读 Tool 只对明确瞬时错误有限重试；写操作使用幂等键、版本校验和操作日志；
7. Approval 保存可审查草稿，确认前重新校验权限、状态和版本，确认前绝不写入；
8. Workflow 保存最小 Run、Step、错误和耗时，日志与摘要不得泄露敏感信息；
9. 不虚构测试结果、指标、引用或已完成功能；未运行的检查必须说明原因；
10. 代码保持模块边界清晰、类型明确、命名可搜索，注释解释业务原因而非代码表面动作。

Python 模块、类、函数、方法的说明性文档字符串及关键流程注释默认使用中文；技术标识、错误码、
日志事件、API 文案、SQL、Prompt 和第三方协议按真实运行契约保留，不得为翻译而改变行为。

## 4. 测试与完成标准

根据当前任务运行 `doc/detailed-plan.md` 指定命令、局部测试、类型/格式检查和必要回归；根级入口按需使用
`make validate`、`make test-tools`、`make test-agent-e2e`、`make eval-*` 等，不要求无关模块重复测试。

完成任务必须满足：

- 正常路径、关键异常和适用质量检查通过；
- `ORDER-003` 黄金场景无无理由回归；
- `docs/STATUS.md` 更新当前进度、实际测试摘要、阻塞和下一任务；
- 测试失败先定位本次影响；既有缺陷或环境阻塞保留证据并如实汇报，不得删测或弱化断言；
- 最终回复列出实际命令和准确结果。

仓库不再维护累计测试报告文件。历史细节由 Git/CI 日志保留，需要指标时运行可重复评测命令；
未经当前真实评测验证的数字不得写入简历或描述为已达到。

## 5. 开发文档门禁

### `doc/detailed-plan.md`

首次开发 `Mx.y` 时，在二级标题下增加 `### 解决的问题`，用1～2句说明本阶段解决的问题和后续能力；
后续仅在问题或边界变化时修正，不写测试数字、文件流水账或未实现能力。

### `doc/record.md`

只记录实际功能里程碑或改变运行行为的核心开发；环境、治理、知识文档、注释语言和纯视觉调整不记录。
每条只允许：

```text
### 核心解决的问题
### 实现的核心代码
### 实现的核心功能
```

二级标题使用日期、任务编号和阶段名称；核心代码只列关键路径和符号，不复制文件，不重复测试、风险、
下一任务或面试内容。历史可为去重和纠错精简，但不得篡改事实。

### `doc/needCare.md`

仅当已实现内容体现 Agent/Tool/Workflow/RAG/上下文/路由/Approval/评测、事实与权限边界，或解决
幻觉、重试、幂等、可观测性等 Agent 工程问题时才记录。普通 CRUD、环境、治理、样板代码和规划不记录。

每条只允许：

```text
### 解决的问题/对项目的价值
### 与 Agent 开发的关系
### 可能的面试问题
```

面试问题必须包含回答要点；设计取舍和未实现边界合并到三栏内，不得过度声称。

## 6. 安全与文件操作

- 不记录或提交密钥、Token、账号、个人信息和生产数据；
- 修改前检查工作区，保留无关用户改动；
- 使用 `apply_patch` 编辑，搜索优先 `rg`；
- 不执行未经明确授权的删除、覆盖、外部写入或破坏性 Git 操作；
- 临时方案必须在最终回复说明原因、覆盖范围、限制、移除条件和后续影响。

## 7. 最终交付

最终回复使用中文并以结果为先，简洁说明：完成内容、关键文件、测试命令与结果、未完成项、已知问题、
替代方案、后续影响、下一最小任务，以及本次是否产生新的 Agent 面试关注点。没有的项目明确写“无”。

涉及代码、接口、Schema、数据库、脚本或配置时，按“整体调用/数据流 → 文件职责 → 核心符号”解释
做了什么、为什么、输入输出和异常边界；只解释本次关键部分。关键位置必须使用完成修改后确认的、
带准确起始行号的绝对路径链接，例如：

```markdown
[Main.java](/absolute/path/business-service/src/Main.java:22)
```

纯文档或配置任务说明规则如何生效、如何验证以及对后续开发的影响。
