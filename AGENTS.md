# AGENTS.md

## 1. 适用范围与指令来源

本文件适用于整个仓库。进入任何子目录开发时都必须遵守；如果将来子目录存在更具体的 `AGENTS.md`，则同时遵守，并以离目标文件最近的规则为准。

开始任务前必须先阅读：

1. `doc/plan.md`：项目需求、架构边界和里程碑；
2. `doc/detailed-plan.md`：任务编号、输入输出、异常场景和停止线；
3. `doc/record.md`：既有实现、关键决策、测试结果和遗留问题；
4. `doc/needCare.md`：已筛选的 Agent 面试关注点和禁止过度声称的能力；
5. 已存在时再阅读 `docs/ROADMAP.md`、`docs/STATUS.md`、`docs/TEST_REPORT.md` 和目标模块的 README。

发生冲突时按以下优先级处理：

```text
用户当前明确要求
→ 本文件/更近目录的 AGENTS.md
→ doc/detailed-plan.md
→ doc/plan.md
→ 现有实现和其他说明文档
```

不得静默猜测重大业务规则或擅自更改接口契约。发现文档矛盾、任务跨越停止线或需要扩大范围时，先说明冲突及影响，再请求确认。

## 2. 项目目标

本项目建设嵌入遥感数据产线业务系统的智能协同 Agent，以订单和生产任务为主线，覆盖订单、生产进度、质检问题、复核结果和成果交付。

第一阶段核心演示问题固定为：

```text
这个订单为什么还没有交付？
```

目标闭环：

```text
读取页面订单上下文
→ 识别诊断意图
→ 通过 Tool 查询订单、任务、进度、质检、复核和交付
→ 通过 RAG 检索适用规范
→ 输出阻塞环节、根因、证据、引用和建议
→ 生成复核意见草稿
→ 用户修改并明确确认
→ Java 服务校验并完成业务回写
```

第一阶段不建设通用 Agent 平台，不提前实现多 Agent、长期记忆、复杂审批/RBAC、工作流拖拽、消息队列、Kubernetes、完整监控平台或多模态影像识别。

## 3. 架构边界

### `web-console`

使用 Vue 3、TypeScript、Vite、Pinia、Element Plus、Axios、SSE、Vitest 和 Playwright。负责页面展示、页面上下文采集、Agent 交互、引用/步骤展示、复核草稿编辑与人工确认。

前端传入的 `order_id`、`task_id`、用户信息和权限只能作为上下文提示；Python 和 Java 必须重新校验。

### `agent-service`

使用 Python 3.12、FastAPI、LangGraph、Pydantic v2、SQLAlchemy 2.x、Alembic、httpx、PostgreSQL/pgvector、pytest、Ruff、mypy 和 uv。

负责上下文、路由、Tool 封装、Workflow/Agent、RAG、Approval、Run/Step 和 SSE。禁止直接读取或修改 Java 业务数据库，所有业务事实和写入都必须通过 Java API Tool。

### `business-service`

使用 Spring Boot 体系和 PostgreSQL，负责业务数据、权限、状态校验、一致性、幂等、操作日志和最终写入。接口必须提供统一错误模型与 Trace ID。

职责原则：

```text
业务事实来自 Java Tool
规范结论来自有版本和引用的 RAG
模型只负责路由、归纳、解释和草稿生成
写操作必须经过人工确认并由 Java 再校验
```

## 4. 不可破坏的业务基线

固定订单及预期结论：

| 订单 | 状态场景 | 预期结论 |
| --- | --- | --- |
| `ORDER-001` | 正常生产 | 无异常，等待生产完成 |
| `ORDER-002` | 生产步骤失败 | 阻塞在生产环节 |
| `ORDER-003` | 坐标系质检问题未关闭 | 质检问题和待复核阻断交付 |
| `ORDER-004` | 问题已处理、复核待完成 | 阻塞在复核环节 |
| `ORDER-005` | 生产、质检、复核完成 | 满足交付条件 |

黄金链路必须始终保持可重复、可重置、可自动测试：

```text
ORDER-003
→ TASK-003 (production_status = COMPLETED)
→ ISSUE-001 (type = COORDINATE_SYSTEM, status = OPEN)
→ review_status = PENDING
→ delivery_status = BLOCKED
```

其诊断基线为：

```text
blocking_stage = QUALITY_REVIEW
根因 = 未关闭的坐标系质量问题 + 质检复核尚未完成
建议 = 创建坐标系处理返工任务 + 处理后重新提交复核
```

除非用户明确批准并同步修改固定数据、预期结果和测试，不得改变上述映射或让模型补造业务事实。

## 5. 里程碑与开发顺序

严格按停止线推进：

| 里程碑 | 目标 | 停止线 |
| --- | --- | --- |
| M0 | 固定数据与 Java 接口 | `ORDER-003` 可通过 Java 接口查询完整数据 |
| M1 | Python Tool 层 | 所有只读 Tool 可由 pytest 独立调用 |
| M2 | 确定性诊断 Workflow | `ORDER-003` 可在前端完成完整诊断 |
| M3 | 页面/会话上下文、路由与澄清 | “这个订单”可安全使用页面 `order_id` |
| M4 | RAG 规范检索 | 诊断引用正确版本的坐标系统规范 |
| M5 | 动态诊断 Agent | `ORDER-001`～`005` 走正确的不同 Tool 路径 |
| M6 | Approval 与安全回写 | 未确认绝不写入，确认后只写一次 |
| M7 | Run/Step、SSE 与统一评测 | 异常有记录且能定位失败步骤 |

固定实现顺序：

```text
Java 接口可验证
→ Python HTTP Client/Tool 可验证
→ 确定性 Workflow 可验证
→ 动态 Agent 决策
```

先读后写。达到当前里程碑停止线后，应先运行完整验收并报告结果，不得自动扩展到下一里程碑。

## 6. 每次开发的执行规则

一次只完成一个有确定输入、输出和测试标准的小任务，通常对应 `doc/detailed-plan.md` 中一个任务编号。用户未给出编号时，根据当前状态定位最小可交付任务，并在开始前明确本次范围和不做事项。

标准流程：

```text
读取需求、历史记录和目标模块
→ 检查工作区并保护用户已有改动
→ 明确任务编号/里程碑、输入、预期输出和异常场景
→ 先编写或确认失败测试
→ 实现范围内的最小改动
→ 运行局部测试
→ 运行适用的类型、格式和质量检查
→ 运行当前停止线要求的回归测试
→ 更新进度/测试文档（存在时）
→ 检查并同步 doc/detailed-plan.md 当前 Mx.y 阶段目标简述
→ 追加 doc/record.md
→ 评估本次开发的 Agent 面试价值，有价值时更新 doc/needCare.md
→ 汇报文件清单、测试结果、风险和下一任务
```

实现要求：

1. 不跨里程碑提前开发，不把多个业务模块塞进一次变更；
2. 不擅自修改 OpenAPI、DTO、数据库字段、统一错误码和状态枚举；
3. 接口契约变化必须同步 Java、Python、前端契约测试及文档；
4. 固定数据禁止随机化，测试不得依赖执行顺序或外部不稳定状态；
5. Tool 输入输出使用明确 Schema，错误至少区分参数错误、资源不存在、权限、超时、上游异常和响应校验失败；
6. 重试只用于明确可重试的读操作；写操作使用幂等键、版本校验和操作日志；
7. Approval 必须保存可审查草稿；确认前重新校验权限、状态和版本，禁止绕过确认或重复写入；
8. 从第一条 Workflow 开始保存最小 Run、Step、错误和耗时信息；
9. 日志、记录、测试夹具和提交中不得写入密钥、Token、个人信息或生产数据；
10. 不虚构测试结果、成功率、性能数据、引用或已完成功能。未运行的检查明确标记“未运行”并说明原因；
11. 首次开发某个 `Mx.y` 阶段时，必须在 `doc/detailed-plan.md` 对应二级标题正下方补充1～2句
    阶段目标简述，说明本阶段搭建什么、解决什么问题以及为后续提供什么能力。后续仅在阶段目标
    或能力边界变化时修正；不得写测试数字、文件流水账、环境信息或尚未实现的能力。

代码应优先保持模块边界清晰、类型明确、命名可搜索。注释解释业务原因和约束，不重复描述代码表面行为。除非任务明确需要，不进行大范围重构或引入新依赖。

Python 模块、类、函数和方法的说明性文档字符串，以及用于解释业务原因、设计约束和关键流程的
注释，默认必须使用中文。FastAPI、Pydantic、SQLAlchemy、Tool、Workflow、Trace ID、字段名、
类名和函数名等技术标识保留原文，确保可搜索和准确。该规则只约束开发说明，不得为了中文化
擅自修改日志事件名、错误码、API 文案、SQL/JPQL、Prompt、第三方协议或其他参与运行的字符串；
运行时文本是否翻译必须依据对应业务契约和当前任务范围单独判断。

## 7. 测试与完成标准

每个任务必须具备：

```text
固定输入
预期输出
正常路径
关键异常路径
自动化测试
独立验收命令
可演示结果
```

优先执行任务在 `doc/detailed-plan.md` 中指定的命令。根级命令建立后，按任务适用范围运行：

```bash
make reset-demo
make test-java-contract
make test-business-data
make test-tools
make test-agent-e2e
make eval-router
make eval-rag
make test-agent-policy
make test-approval
make eval-all
make quality
```

完成定义：

1. 实现符合当前任务输入、输出和架构边界；
2. 正常与异常测试通过；
3. 适用的 Ruff、mypy、Java/TypeScript 检查通过；
4. `ORDER-003` 黄金场景没有无理由回归；
5. 状态、测试报告与 `doc/record.md` 已更新；
6. 没有把计划外工作伪装为“顺手修复”；
7. 最终报告给出实际运行的命令和准确结果。
8. 已完成 Agent 面试价值评估；有价值的内容已写入 `doc/needCare.md`，无价值时未制造空条目。
9. 已检查当前 `Mx.y` 在 `doc/detailed-plan.md` 中的阶段目标简述；首次开发已补充，目标或边界
   变化时已同步，且该简述没有替代 `doc/record.md` 的实现级历史记录。

测试失败时先定位并修复本次引入的问题。若失败来自既有缺陷或环境阻塞，保留证据并如实记录，不得删除、跳过或弱化测试来制造通过结果。

## 8. 强制开发记录

每一次修改代码、配置、接口、数据模型、测试或重要开发文档，都必须在结束前追加到 `doc/record.md`。该记录是完成定义的一部分；没有记录，任务不得宣称完成。

记录要求：

1. 只能追加新的日期/任务条目，不覆盖或改写历史事实；历史内容确需纠正时，追加“更正”条目并引用原条目；
2. 记录任务编号、里程碑、目标、范围和关键业务判断；
3. 记录核心代码的文件路径、类/函数/接口/Schema 名称及其职责；只摘录必要的关键片段，避免复制整文件；
4. 记录重要契约、状态流转、数据映射、算法/流程、异常与安全处理；
5. 记录所有实际执行的命令、通过/失败数量和未执行项；
6. 记录兼容性影响、已知风险、遗留问题和建议的下一小任务；
7. 不得记录密钥、Token、账号、生产数据或未经脱敏的请求响应。

统一使用 `doc/record.md` 中的模板。纯问答或只读分析没有产生仓库变更时无需追加。

如果实现使用临时替代方案、降级方案或技术占位，记录中还必须说明：

1. 为什么不能直接采用目标方案；
2. 替代方案实际覆盖了哪些验收要求、没有覆盖哪些要求；
3. 替代方案的限制、风险和移除条件；
4. 对后续任务、里程碑、接口兼容、数据迁移、测试或部署的具体影响。

替代方案只能作为透明、可追踪的阶段性选择，不能把“部分可用”记录为“完整实现”。

## 9. Agent 面试价值评估

`doc/needCare.md` 不是开发流水账。每次开发结束前必须评估本次内容对 Agent 岗位
面试是否具有实际价值，只有满足以下至少一项时才更新：

1. 体现 Agent、Tool、Workflow、RAG、上下文、路由、Approval 或评测设计；
2. 体现模型与业务事实、权限、安全或写操作之间的边界；
3. 解决幻觉、可重复性、错误处理、重试、幂等或可观测性问题；
4. 存在能够基于真实实现说明的技术取舍、工程难点或故障定位；
5. 明确影响后续 Tool 路径、Schema、评测指标或 Agent 结果可靠性。

普通文件调整、无 Agent 关联的配置、环境安装、提示词/文档治理、常规 CRUD、
字段搬运、样板代码和尚未实现的规划默认不记录。不得为了“每次都写”制造空条目，
也不得将计划能力描述为已实现。

有效条目应说明 Agent 面试价值、原理和取舍、可能问题与回答要点、必要的简历表述，
以及尚未完成、不能过度声称的能力。没有可靠简历价值时不强行提供表述。

`doc/record.md` 继续记录每次开发的完整事实；`doc/needCare.md` 只保存通过上述门禁
筛选后的面试知识，两者不能互相替代。

最终回复必须说明本次是否产生新的 Agent 面试关注点及判断理由。若没有价值，只在
最终回复中说明，不修改 `doc/needCare.md`。

## 10. 进度文档与最终汇报

当对应文件存在时，每完成一个任务同步更新：

```text
docs/ROADMAP.md：里程碑及任务勾选状态
docs/STATUS.md：当前里程碑、已完成任务、通过/失败测试、阻塞和下一任务
docs/TEST_REPORT.md：只写真实运行所得的指标
doc/detailed-plan.md：当前 Mx.y 标题下的简短阶段目标和能力边界
doc/record.md：不可省略的实现级历史记录
```

最终回复使用中文并以结果为先，至少说明：

1. 完成了什么及业务效果；
2. 修改了哪些文件；
3. 执行了哪些测试及结果；
4. 未完成项；
5. 已知问题或阻塞；
6. 使用过的替代方案、采用原因、局限和转正/移除条件；
7. 对后续开发、兼容性、数据、测试和部署的影响；
8. 建议的下一项最小任务。
9. 本次是否产生新的 Agent 面试关注点及判断理由。

以上第 4～7 项即使没有内容，也必须分别明确写“无”，不得省略。开发中发现但已修复的问题也要简述问题、修复方式和是否仍有残余影响。

### 代码解释与定位要求

凡任务涉及代码、脚本、配置、接口、Schema、数据库模型或关键测试，最终回复还必须提供有层次的实现说明：

1. 先解释整体目标、模块边界和调用/数据流，再解释各文件职责，最后解释核心类、函数、接口或配置项；
2. 说明关键实现“做了什么、为什么这样做、输入输出是什么、异常和边界如何处理”，不能只罗列文件名；
3. 说明核心代码与业务对象、固定数据、接口契约或当前里程碑的对应关系；
4. 对临时占位或替代实现，明确目标实现、当前差异、替换位置和迁移注意事项；
5. 关键文件必须提供可点击的绝对路径 Markdown 链接，并带准确起始行号，例如：

   ```markdown
   [Main.java](/absolute/path/business-service/src/Main.java:22)
   ```

6. 行号必须在完成全部修改后重新读取文件确认，禁止凭记忆填写；优先链接到类、函数、接口、Schema、配置段或测试用例的定义行；
7. 同一文件存在多个关键位置时，可按主题列出多个行号，但避免逐行解释和无意义重复；
8. 纯文档、环境配置或没有业务代码的任务，也要解释关键配置如何生效、由谁读取、验证方式以及对后续开发的影响。

代码解释是交付内容的一部分。缺少关键逻辑说明或无法直接定位的文件链接时，不得将任务视为完整交付。
