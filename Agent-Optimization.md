# Agent 面试问题对应的项目清单

本文件只记录梳理问题和答案时发现的项目描述修改、当前边界、验证事项、优化点和待实现功能，不展开实现方案。当前优先级是先完成问题和答案梳理，再按本清单补强项目。

## 一、项目描述修改

1. 使用“跨业务模块联合诊断”，不描述为多个独立系统集成。[001]
2. 自然语言入口可以使用受控模型识别意图，路由后简单查询使用固定 Workflow，动态 Agent 仅用于取证路径不固定的复杂诊断。[001-1][003-1]
3. 模型负责意图理解、动态取证和草稿生成，Java 提供业务事实与最终写入，规则生成正式诊断，写操作需要人工确认。[001-2][004-4]
4. 当前实现范围结束于安全创建状态为 `PENDING` 的返工任务，不描述为完成最终交付闭环。[004-8]
5. 区分 Stub、受控评测和真实模型结果，不把框架验证描述成真实模型效果。[001-3]
6. 项目定位为面向遥感数据生产订单的全流程协同 Agent，覆盖状态查询、任务跟踪、联合诊断、规范问答、复核辅助和受控返工，`ORDER-003` 作为代表性黄金链路。[003]
7. 项目基于公司业务背景进行工程验证，不把业务背景直接描述为已经生产上线的成果。[003]
8. “单主 Agent + 多 Skill”表示统一入口负责上下文、路由和 Run，四个受控业务 Skill 分别承载状态查询、动态诊断、规范问答和复核草稿。[006]
9. BusinessSkill 采用“文字引导模型、代码控制执行”的混合实现，意图和动作描述用于理解与规划，Schema、权限、Tool 白名单、Workflow 和 Java 校验负责实际执行边界。[006-1]
10. Tool 不只是 HTTP API 的 Schema 描述，而是由 Registry、Workflow 策略门禁、BaseTool 通用门禁、HTTP Client 契约和 Java 最终校验共同组成的受控执行协议。[011]
11. 动态诊断使用本轮 Java Tool 返回的关联任务列表作为后续任务级动作的资源范围，模型不能仅凭格式合法的任务号跨订单查询。[011-1]
12. 只有 Java 返回完整契约和显式空集合时才能解释为空业务事实，HTTP 200 中的缺字段、`null` 或类型错误必须作为响应契约失败。[011-2]
13. 静态动作映射定义模型协议允许的动作与 Tool 对应关系，Registry 则保存当前进程实际装配的 Tool 实例及其执行契约。[011-3]
14. 规范问答使用 RRF 融合关键词与向量排名并截取候选，再使用独立的 Rerank 相关性分数完成精排和低相关内容过滤。[019]
15. RRF 只累加候选实际出现通道的排名贡献，因此在当前 `k=60` 下两路均为第三名的候选会排在仅有一路第一名的候选之前。[019-1]
16. 同一 Chunk 被两路召回时必须通过完整载荷一致性校验，相邻不同 Chunk 则按文档、章节和连续位置合并为保留全部来源的段落级候选。[019-2]
17. Rerank 将输出不完整、调用超时和非超时执行异常分别处理为响应校验失败、结构化安全降级和稳定执行错误。[019-3]

## 二、当前项目边界

1. Turn 当前不是独立持久化实体，而是通过用户消息和 Run 表示一轮交互。[004-1]
2. 复核草稿依赖同一 Session 中最近一次成功诊断，尚不支持直接请求复核后的自动串联。[004-4]
3. 确认时会刷新 Java 事实和任务版本，但不会重新执行 RAG。[004-5][007-3]
4. 最终诊断证据只包含 Java Tool 事实，诊断阶段的 RAG 结果不会进入最终规范引用。[004-7]
5. 诊断规范缺口目前只判断检索结果是否存在。[004-7]
6. `confidence` 当前是 `1.0/0.0` 的二值完整度标记，不是校准后的模型概率。[004-7]
7. 提交复核只新增复核记录，创建返工还要求最新有效复核结论明确需要返工并经过独立请求与确认，不会自动关闭问题或改变交付状态。[004-8][003-3]
8. 返工目前只支持坐标系类型，创建后状态为 `PENDING`，不代表返工完成或订单可以交付。[004-8][003][003-3]
9. Java 写入成功但 Agent 保存失败时，Approval 可能停留在 `EXECUTING`，当前没有自动恢复机制。[004-9]
10. 页面上下文只作为查询提示，不能作为业务事实或权限依据。[004-10][007]
11. 当前使用演示 Header 表示身份和角色，不是生产级认证。[004-10][003]
12. 动态诊断的真实模型泛化、耗时和成本尚未完成完整验证。[001-3][003]
13. 当前路由协议、结构化解析和异常回退已有受控测试，但真实模型的路由准确率、耗时和成本尚未完成完整验证。[003-1]
14. 当前四个 Skill 是由意图目录、统一分发器和下游 Workflow 组成的逻辑业务执行单元，不是四个独立 Skill 类或自治 Agent。[006]
15. 当前统一入口生产主链路中的 LangGraph 用于动态诊断和规范问答的进程内状态与分支流转，状态查询与复核草稿使用普通固定 Workflow，并且尚未实现 checkpoint、重启恢复或多 Agent 协作。[006][006-2][006-3]
16. 当前没有独立的 Markdown 文字 Skill 加载和版本机制，相关文字说明位于意图目录和模型 Prompt 中。[006-1]
17. Session 只保存最小结构化指代和澄清上下文，不复制 Java 业务事实，也不把完整聊天记录作为长期语义记忆。[007][007-2][007-3]
18. 用户明确实体会在路由决策形成后、业务 Skill 执行前写入 Session，因此 Session 指代不代表资源已经通过 Java 验证。[007]
19. Run 和 Step 保存当次执行的输入快照、过程与结果证据，不能代表当前 Java 业务状态。[007]
20. 用户仅通过消息切换订单时，当前 Session 更新尚未在所有情况下清除 `current_task_id` 和 `issue_id`，可能暂时形成新订单与旧下级对象混合的指代。[007-1]
21. 当前主要将用户原始消息保存到 AgentMessage、将模型最终结果保存到 Run，但不会把完整历史记录自动提供给后续模型调用。[007-2]
22. Session 过期后会拒绝继续访问，但当前没有自动物理清理过期 Session 和消息的后台任务。[007-2]
23. SessionContext Schema 不定义生产、质检、复核和交付状态并禁止额外字段，数据库中的上下文不符合契约时会被拒绝使用。[007-3]
24. 动态诊断当前使用结构化 `ActionDecision` 和本地执行器调用 Tool，不是供应商原生 `tools/tool_calls` 协议。[011]
25. `BaseTool` 负责通用执行契约但不自动持久化 Run/Step，统一生产链路由观测包装器另行记录 Tool Step。[011]
26. 动态诊断的资源范围不匹配当前收敛为 `PARAM_VALIDATION_ERROR`，独立任务跟踪没有订单上下文时则主要依赖 Java 校验任务存在性和访问权限。[011-1]
27. `RESPONSE_VALIDATION_ERROR` 当前为不可重试错误并使当前 Skill 明确失败，而响应 Schema 合法本身也不能独立证明业务内容真实。[011-2]
28. 当前 Registry 只是进程内名称到实例的映射，不提供远程服务发现、网络健康检查或动态热加载。[011-3]
29. 动态诊断查询到未注册 Tool 时当前收敛为 `PARAM_VALIDATION_ERROR`，虽能阻断执行但未单独表达能力未装配。[011-3]
30. 关键词与向量通道默认各召回最多 20 条，RRF 去重和相邻片段合并后只按排名截取最多 10 条，不使用 `0.5` 作为 RRF 阈值。[019]
31. `0.5` 是 Rerank 的默认相关性过滤阈值而非概率或 RRF 门槛，当前实现不直接比较或加权混合两种分数。[019]
32. Rerank 超时会在组件层保留 RRF 候选并标记降级，但规范问答会返回 `RERANK_UNAVAILABLE`，不会使用这些候选生成规范结论。[019][019-3]
33. RRF 常数 `60` 与 Rerank 阈值 `0.5` 当前是固定工程参数，真实 Provider 的重排质量、耗时和成本尚未完成验证。[019]
34. RRF 高分只表示多个召回通道的排名共识，不等同于已经证明候选与当前问题相关，候选仍需经过 Rerank。[019-1]
35. 同一 `chunk_id` 的文档、版本、位置、章节、正文或哈希不一致时当前会使整次融合失败，而不会选择任一通道结果继续执行。[019-2]
36. 相邻 Chunk 合并按组内各通道最佳排名重新计算段落级 RRF，可能汇聚来自不同片段的召回信号并提高合并结果排序。[019-2]
37. Rerank 输出缺失、重复或未知候选 ID 时会作为不可重试的 `RERANK_RESPONSE_VALIDATION_ERROR` 使当前规范问答 Skill 失败。[019-3]
38. Rerank 非超时调用异常会隐藏供应商原始信息并转换为不可重试的 `RERANK_EXECUTION_ERROR`，不会形成规范回答或引用。[019-3]

## 三、验证与证据补充

1. 为状态查询、复杂诊断、规范问答和复核辅助分别准备可演示案例。[001][003][004]
2. 使用生产中、生产失败、质检阻塞、复核阻塞和可以交付等状态验证不同动态取证路径。[001-1][003]
3. 使用相同输入、Tool、数据和规则，对比固定诊断 Workflow 与动态 Agent。[001-3]
4. 使用真实模型验证至少一条动态诊断链路。[001-3]
5. 验证 Run/Step 能展示 Tool 选择、停止原因和失败位置。[004-1][007]
6. 依据已确认的业务需求、领域规则和接口契约预先定义状态矩阵，并使用重复确认、版本冲突和 Tool 失败等反例检验 AI 生成的实现与测试。[003][003-2]
7. 使用真实模型评测意图路由的准确率、澄清与 `UNKNOWN` 回退、耗时和成本。[003-1]
8. 为关键业务规则补充系统化变异测试和独立业务验收记录。[003-2]
9. 为新增返工类型验证有效复核前置条件、结论约束、版本失效、重复确认幂等和创建后状态不联动等边界。[003-3]
10. 如果未来引入多 Agent，需在相同数据、Tool、权限、模型和业务规则下与“单 Agent + 多 Skill”基线对照，同时验证任务质量、安全性、协作失败和运行成本。[006-3]
11. 验证本轮明确消息、已确认 Session、页面提示和会话候选的实体优先级、同级冲突澄清以及 Java 资源重校验。[007][007-1]
12. 验证只切换订单时清空旧下级对象、同时切换订单和任务时保留新任务、任务与订单不匹配时由 Java 拒绝。[007-1]
13. 验证路由模型不会隐式读取完整消息历史，并覆盖 Session 所有者校验、过期拒绝和显式 Run 关联。[007-2]
14. 验证 Session 拒绝额外业务状态字段，并在跨轮续接时重新查询 Java 而不是复用历史 Run 中的状态。[007-3]
15. 使用真实 Provider 和业务标注集验证 Rerank 的排序质量、低相关过滤、超时行为、耗时与成本，并区分受控结果和真实效果。[019]
16. 在 RRF 单测中保留单路第一与双路第三的精确算例，锁定缺失通道不计分和双路共识排序语义。[019-1]
17. 持续验证同 ID 载荷冲突会失败关闭，并覆盖相邻、跨章节和位置不连续片段的合并边界。[019-2]
18. 持续覆盖候选缺失、重复与越界、超时安全回答和非超时异常脱敏，确认三类失败不会进入规范回答生成。[019-3]

## 四、现有能力优化

1. 校验诊断 RAG 的返回状态和引用充分性，并决定是否在诊断结果中展示规范引用。[004-7]
2. 在确认阶段校验草稿使用的规范版本和引用是否仍然有效。[004-5]
3. 调整或重新定义二值 `confidence`，避免被理解成模型概率。[004-7]
4. 为 `STALE` Approval 明确设计新草稿、新 Run 和新 Approval 的重建流程。[004-5]
5. 修复 Session 在订单切换时的父子指代更新顺序，完整清除旧任务和问题后再写入本轮明确的新下级对象。[007-1]
6. 基于真实业务标注集比较融合与重排效果，并按评测结果决定是否调整 RRF 常数、候选数量和 Rerank 阈值。[019]
7. 评估相邻片段汇聚各通道最佳排名是否会放大段落级 RRF，并根据真实检索评测决定是否调整合并评分策略。[019-2]
8. 为 `RerankValidationError` 定义显式稳定错误码和重试属性，避免生产 Skill 依赖 `getattr` 默认值完成错误映射。[019-3]

## 五、待实现功能

1. 用户直接请求复核时，自动执行诊断 Run，再创建复核 Run。[004-4]
2. 实现返工执行、问题状态流转、重新复核和交付条件重算。[004-8]
3. 实现超时 `EXECUTING` Approval 的原幂等键重放、对账和终态恢复。[004-9]
4. 使用生产级身份认证替换演示 Header。[004-10]
5. 扩展坐标系之外的返工类型，并优先补充几何精度问题的规范、诊断建议、复核和受控返工链路。[004-8][003-3]

## 六、后续同步规则

1. 每次向 `Agent-Answer.md` 同步成熟答案时，同时检查是否产生项目描述修改、当前边界、验证事项、优化点或待实现功能。
2. 新内容按类型加入对应章节，并保持连续编号。
3. 每条只写一句话，并在结尾标注来源；主问题使用 `[001]`，具体追问使用 `[004-2]`，多个来源连续标注。
4. 已存在的内容直接修改原条目，不重复追加。
5. 不在本文件展开实现方案、代码路径或长篇分析。
6. 项目完成相关功能后，从“待实现功能”或“现有能力优化”中删除对应条目，并同步修正“当前项目边界”。
